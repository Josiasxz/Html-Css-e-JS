// Função inteira arrow function
const sum = (a, b) => {
    return a + b;
}

// Função resumida
const som = (a,b) => a + b;

// ------------------- Transformar função em arrow function ----------------------------

// Função
function isAdult(age)
{
    return age >= 18;
}

// Arow Function

const isAdult2 = (age) => 
{
    age >= 18;
}